---
title: "LLM APIの個人デモを公開するとき、キーを隠すだけでは足りなかった"
emoji: "🔑"
type: "tech"
topics: ["claude", "vercel", "llm", "個人開発", "セキュリティ"]
published: false
---

## この記事の対象と、扱う規模

個人開発でLLM APIを使い始めて、作ったものをインターネットに公開しようとしている人向けです。

先に規模感を正直に書いておきます。この記事は**個人ポートフォリオのデモを公開する話**であって、本番運用のセキュリティ設計の話ではありません。実際にやったのは、ダミーデータで動くデモを1本、事故らない形で公開しただけです。企業システムの水準を期待して読むと物足りないはずなので、そこは最初に断っておきます。

それでも書く理由は、この規模でも「知らないと確実に踏む穴」がいくつかあって、しかもそれが入門記事だとあまり書かれていないと感じたからです。

## 出発点：ブラウザから直接APIを叩いていた

作っていたのは、介護施設の空き枠と利用希望をマッチングして、連絡文の下書きを生成するアプリです。Reactで作って、手元では問題なく動いていました。

ただ、この時点の構成はこうでした。

```
ブラウザ → Anthropic API
```

ブラウザ上のJavaScriptから直接APIを呼んでいる。手元で自分だけが使う分にはこれで動きます。問題は公開したときで、この構成だとAPIキーがブラウザに送られるので、開発者ツールを開けば誰でも読めます。読まれたら、そのキーで好きなだけAPIを叩かれて、請求は私に来ます。

ここまでは調べればすぐ出てくる話です。私が引っかかったのはこの先でした。

## キーを隠しても、課金は止まらない

対策としてサーバーを1段挟みます。

```
ブラウザ → 自分のサーバー → Anthropic API
```

キーはサーバー側の環境変数に置いて、ブラウザには渡さない。これでキーは盗まれなくなります。

ただ、**キーが盗まれないことと、お金が減らないことは別**です。

公開されているのは中継サーバーのURLです。ブラウザで通信を見れば「このURLにこう投げれば文章が返ってくる」はすぐ分かります。あとは自分のプログラムからそのURLを叩き続けるだけで、キーを一度も見ることなく、私のキーで生成した結果が無限に手に入る。いわゆるオープンプロキシの状態です。

インターネットに公開されたエンドポイントは自動スキャンで見つかるものだと考えた方がいいと思っています。GitHubに漏れたAPIキーが短時間で悪用されるといった報告もあり、「小さい個人サイトだから誰も来ない」という前提は成り立ちません。

つまりキーを隠すのは「泥棒に鍵を渡さない」対策で、それだけでは「口座から金が抜ける」方は止まりません。ここを分けて考えられていなかったのが、私の最初の誤解でした。

## 上限を3か所にかけた

そこで、コストが青天井にならないように上限を重ねました。ポイントは、**破られたときに何が最後の壁になるか**を決めておくことです。

### 1. 出力の上限（max_tokens）

サーバー側で1024に固定しました。抽出も連絡文の生成も短い出力しか必要ないので、これで足ります。

重要なのは、**クライアントからmax_tokensを受け取る口を作らないこと**です。リクエストボディに直書きして、外から値を渡せないようにしています。上限を設けても迂回できる形になっていたら意味がありません。

### 2. 入力の上限

出力だけ絞っても、巨大な入力を投げられれば入力側のトークン課金が膨らみます。こちらもサーバーで弾くようにしました（記録文は4000文字まで、超えたら400を返す）。

### 3. 残高そのもの

一番効いたのはこれです。Anthropicの管理画面（platform.claude.com）は前払いクレジット制で、**チャージした分を超えては絶対に使われません**。$5だけ入れておけば、どんな事故が起きても損失は$5で止まります。

ひとつだけ確認が必要なのが**自動リロード（auto-reload）**の設定です。これがオンだと残高が減るたびにカードから補充されるので、壁が壁でなくなります。私の環境では初期状態でオフでしたが、公開前に必ず見ておくべき箇所だと思います。

この3つのうち、1と2は「1回あたりのコストを頭打ちにする」もの、3は「総額を頭打ちにする」ものです。役割が違うので、どれか1つでは足りません。

## レート制限は、穴があると分かったうえで塞がなかった

レート制限も入れました。IPごと20回/10分、全体200回/10分です。

ただしこの実装には既知の穴があります。カウントをサーバーのメモリ上に持っているのですが、Vercelのようなサーバーレス環境では関数のインスタンスが複数立ち上がることがあり、**インスタンスごとに別のメモリを持つ**ので、カウントが共有されません。インスタンスが5つに増えれば、実効的な制限は5倍に緩みます。ベストエフォートです。

きちんと塞ぐなら、RedisやVercel KVのような外部の共有ストアにカウントを置く必要があります。

**それをやりませんでした。** 理由は、上限が$5だからです。

仮に制限が5倍に緩んで10分で1000回叩かれたとしても、この構成のコストは1回あたり1円弱（Haikuは100万トークンあたり入力$1・出力$5で、出力を1024に絞っているため）。10分で1000円弱が最悪ケースで、しかもその手前で残高$5が尽きて止まります。つまり**穴が空いても損失の上限は動きません**。

一方で共有ストアを入れると、外部サービスの追加、接続設定、レイテンシ、無料枠の管理が一式ついてきます。個人デモに対しては割に合わないと判断しました。

これは「厳密なレート制限は不要」という主張ではありません。**最後の壁が別にあるから、この層は緩くてよい**という判断です。本番で課金が青天井になる構成なら、当然ここを堅くする必要があります。

## やったこと・やらなかったことの整理

やったこと：

- キーをサーバー側に隠す（ブラウザに渡さない）
- 出力トークン上限をサーバーで固定、クライアントから指定不可に
- 入力サイズの上限をサーバーで検証
- レート制限（インメモリ、ベストエフォート）
- クレジット残高を少額に固定、自動リロードは無効
- `.gitignore` に `.env` があることを目視で確認してから公開

やらなかったこと：

- レート制限の共有ストア化
- 認証（デモなので誰でも触れる状態にした）
- WAFやbot対策の導入
- 本番相当の監視・アラート

そして、この「やらなかったこと」と理由をREADMEにも書きました。制限を知らずに素通りしたのか、知ったうえで選ばなかったのかは、外からは区別がつかないからです。

## まとめ

- キーを隠すことと、コストを止めることは別の対策
- 公開されたURLは、キーがなくても叩かれ続ける
- 上限は「1回あたり」と「総額」の両方に必要
- 一番強い壁は、支払える金額そのものを絞ること
- どこを堅くしてどこを緩めたかを、理由つきで書いておく

繰り返しになりますが、これは個人デモの水準の話です。業務システムで同じ構成にしてよいという話ではありません。ただ、個人開発でLLM APIを触り始めた段階でも、ここまでは考えておいた方がいいと思っています。
