# kaigo_matching — プロジェクト指示

## このプロジェクトについて

短期入所（ショートステイ）施設の空床マッチング支援アプリ。
利用記録を貯めると、居宅介護支援事業所（ケアマネ）の傾向が自動集計され、
空き枠を埋めるときに「どこの誰に声をかけるか」の優先順位と、連絡文の下書きが出る。

※ グローバル設定（`~/.claude/CLAUDE.md`）の4原則はそのまま適用される。
　 ここにはこのプロジェクト固有のことだけを書く。

## 構成

```
src/App.jsx           本体アプリ（React・単一ファイル）
eval-app.jsx           記録抽出eval
eval-cases.json        抽出evalの正解データ
draft-eval.jsx          連絡文eval（LLM-as-judge）
```

- 介護ドメインの判断基準は `~/.claude/skills/kaigo-domain-knowledge/SKILL.md` にある。
  記録の読み方・信頼スコアの根拠に関わる作業では、必ずそちらを参照すること。

## データ構造

利用記録1件（`window.storage` の `usage-records-v1` に保存）:

| フィールド | 内容 |
| --- | --- |
| date | 利用日 |
| kyotaku | 紹介元の居宅・ケアマネ事業所名 |
| staff | 紹介元の担当ケアマネ（姓のみ・敬称なし） |
| care | 要介護度 |
| medical | 医療処置の要否 |
| result | regular（定期）/ single（単発）/ watch（様子見） |
| trouble | トラブルの有無 |
| discrepancy | 事前情報と実際の食い違い |
| memo | 現場の様子 |

差出人設定は別キー（`sender-settings-v1`）で保存する。
**記録を全消ししても施設情報は残す**という意図なので、同じキーにまとめないこと。

### 集計の考え方
- **信用スコア = 事前情報とのズレの少なさ**。現場で最も困るのがズレのため、単独で追う。
- **トラブル率 = それ以外の問題（大小まとめて）**。約束の不履行（連絡なしに提供表へ予約を入れる等）もここに含める。
  種類を細かく分類しないこと。**記録が面倒になると現場で使われなくなる**ため。
- 数字だけでは判断できないので、**ズレ・トラブルの実際のメモを併記する**。数字は入口、中身で最終判断。
- 担当者別の集計は**居宅の中でのみ**行う（別居宅の同姓を混ぜない）。
- **3件未満の担当者は「データ少」として断定を避ける**。少数事例で決めつけない。

## 連絡文プロンプトの制約（重要・変更禁止）

`generateDraftAI` のプロンプトにある以下の条件は、**連絡文evalで実際に問題が出たため追加されたもの**。
消したり緩めたりすると、過去に確認済みの不具合が再発する。

1. **「短期入所」「ショートステイ」の語を書かない**
   → 指定していないのにAIが勝手に補っていた。相手（ケアマネ）は承知しているため不要。
2. **連絡手段（電話・FAX・メール等）を書かない**
   → FAXを禁止したら「電話・メッセージ」で再発した。手段は一切指定していないため、書けば必ず推測になる。
   →「ご連絡ください」までは可。
3. **差出人（施設名・担当者名）は設定値をそのまま使い、創作しない**
   → 渡していなかった頃、AIは実在しない施設名・人名を名乗っていた。危険。
   → **差出人未設定のときは連絡文を生成させない**（UI側でブロック済み）。
4. **空き枠は開始日〜終了日の期間で書く。曜日だけの表現は使わない**
   → 「直近の水曜」ではどの週か分からず誤解を生む、という現場からの指摘による。
5. **内部情報（相手の信用度・過去のトラブル・実績の有無）を漏らさない。釘刺しもしない**
   → 要注意の相手でも文面は通常どおり淡々と。要注意なら「そもそも送らない」という運用のため、
     AIが気を利かせる必要はない。
6. **年（西暦）は書かなくてよい**
   → 来年の予約は基本ないため、現場では不要。

### 根本原則
**AIが捏造したら、まず「自分が情報を渡していないこと」を疑う。**
禁止事項を足していくモグラ叩きより、必要な事実を渡すほうが根本的な解決になる。

## eval の運用ルール

### 判断基準（介護現場の感覚に基づく）
- **軽微な様子の変化は「食い違い」に含めない**（例：少し表情が硬い／食事が少しゆっくり）。
  一方、**実質的な負荷差は含める**（例：想定より介助量が多い）。
- **バイタルは絶対値で異常判定しない。その人の平常値との比較で見る。**
- 「居宅」は「居宅介護支援事業所」の略称。**表記ゆれは同一とみなす。**
- 「ご連絡ください」は連絡文の用件そのもの。捏造ではない（手段の明記のみNG）。

### 結果の読み方
- **1回の満点を信じない。** 複数回実行して平均・最低・ブレを見る。
  過去に「1回は正解、10回では0/10」というケースが実際にあった。
- **完走率90%未満の結果は信用しない。** 通信エラーで欠けたデータの平均は実力ではない。
  完走できない10回より、完走できる5回のほうが正直。
- **総合点が高くても criticalMisses（重大な見逃し）を必ず確認する。** 平均点に埋もれる危険な誤りを拾うための仕組み。
- **LLM-as-judge（AI審判）自体を疑う。** 審判は過去に5回修正している。
  「全項目クリア」は「問題がない」ではなく「審判が見つけられていない」可能性がある。
  審判を変更したら、必ず人間が生成文と判定を目視で突き合わせること。

### eval の変更時
- 抽出evalのケース追加は `eval-cases.json` を編集するだけでよい（アプリのコード変更は不要）。
- 各ケースには `intent`（何を試したいか）を必ず書く。後から見て意図が分からなくなるのを防ぐため。
- **アプリに新機能を足したら、evalも追従させる。** 未検証の機能を積み上げない。

## 開発上の注意

- 本体・evalともに**単一ファイルのReactコンポーネント**。artifactとしても動く形を維持している。
- **ブラウザのlocalStorage/sessionStorageは使わない。** 永続化は `window.storage` を使う。
- コード変更後は構文チェックを通すこと:
  `npx esbuild <file>.jsx --loader:.jsx=jsx --bundle --external:react --outfile=/dev/null`
- 送信機能はデモ（実際には飛ばない）。実装する場合は人間の承認フローを必ず残すこと。

## 文章を書くとき

Zenn記事・READMEなどを書く場合は `~/.claude/skills/zenn-writing/SKILL.md` を参照。
**盛らない**（誇張しない・できなかったことは正直に書く）が全体の背骨。
